# 내 PC(로컬)에서 실행하기 — 완전 초보 가이드

코드스페이스 없이 **내 컴퓨터에서 직접** 웹앱을 돌리는 방법입니다.
로컬 실행은 인터넷 포워딩이 없어서 **중간에 끊길 일이 없고, 무료 시간 제한도 없습니다.**

---

## 1. Python 설치 (처음 한 번만)

1. https://www.python.org/downloads/ 접속
2. **Python 3.12.x** 다운로드 버튼 클릭 (3.11~3.12 권장. 3.13 이상은 일부 패키지 문제 가능)
3. 설치 시작 화면에서 ⚠️ **"Add python.exe to PATH" 체크박스 반드시 체크** → Install Now
4. 설치 끝나면 확인: `Win키 + R` → `cmd` 입력 → 검은 창에서
   ```
   python --version
   ```
   → `Python 3.12.x` 나오면 성공

## 2. 프로그램 내려받기 (처음 한 번만)

1. https://github.com/bibleheo/nice-bizline-app 접속
2. 왼쪽 위 브랜치 버튼(`main ▾`) → **`claude/nice-bizlin-app-t82c1b`** 선택
3. 초록 **`<> Code`** 버튼 → **Download ZIP**
4. 받은 ZIP을 원하는 폴더에 **압축 해제** (예: `C:\nice-bizline`)

> 나중에 코드가 업데이트되면 ZIP을 다시 받아 덮어쓰면 됩니다.

## 3. 실행 (매번 이것만)

압축 푼 폴더에서 **`웹앱_실행.bat` 더블클릭**

- 처음 한 번은 패키지+크롬 설치로 **5~10분** 걸립니다 (이후엔 몇 초)
- 검은 창에 "웹앱 시작" 나오고 → **브라우저가 자동으로 열립니다** (http://localhost:8501)
- 안 열리면 브라우저 주소창에 직접: `localhost:8501`

## 4. 사용법 (웹 화면에서)

1. 왼쪽 사이드바 **"모의 모드" 체크 해제**
2. 나이스비즈라인 **ID / PW** 입력
3. 회사 목록 **엑셀 업로드** (헤더: 회사명 필수, 사업자번호/대표자명/주소 있으면 정확도↑)
4. **중복 필터** 체크 확인 (대표자명/주소 있으면 자동 표시)
5. **▶ 조회 시작** → 끝나면 **결과 엑셀 다운로드**

## 5. 종료

- 검은 창(터미널)을 닫으면 웹앱이 꺼집니다.

---

## 문제 해결

| 증상 | 해결 |
|------|------|
| `python은(는) 내부 또는 외부 명령...` | Python 설치 시 PATH 체크 안 한 것 → Python 재설치(PATH 체크) |
| 패키지 설치 실패 | Python 3.12로 재설치 (3.13/3.14는 미지원 패키지 가능) |
| 브라우저 자동으로 안 열림 | 주소창에 `localhost:8501` 직접 입력 |
| 실행 중 PC가 잠자기 모드 | 전원 옵션에서 절전 끄기 (장시간 배치 시) |
| 사이트 로그인 실패 | 본인 브라우저에서 나이스비즈라인 로그아웃 후 재시도 (동시접속 1명 제한) |

## 코드스페이스와 차이

| | 로컬 PC | 코드스페이스 |
|--|--------|------------|
| 끊김 | 없음 ✅ | 브라우저 연결 가끔 끊김 |
| 시간 제한 | 없음 ✅ | 월 60시간 무료 |
| 설치 | 처음 한 번 필요 | 자동 |
| 접속 IP | 내 회사/집 IP | 미국 서버 IP |
